---
phase: 06-quiz-sections
plan: 06
type: execute
wave: 1
depends_on: []
files_modified:
  - src/state/showStore.ts
  - src/components/operator/sections/AskIntelligentlyPanel.tsx
  - src/components/audience/sections/AskIntelligentlyDisplay.tsx
autonomous: true
gap_closure: true

must_haves:
  truths:
    - "Audience display shows a 9x8 grid of 72 animal cells overlaid on the composite image"
    - "Operator can click any animal cell to mark it as revealed/asked"
    - "Revealed animals show a visual indicator (highlight/dim) on both operator and audience displays"
    - "Per-animal reveal state syncs from operator to audience via BroadcastChannel"
  artifacts:
    - path: "src/components/audience/sections/AskIntelligentlyDisplay.tsx"
      provides: "72-cell CSS grid overlay on composite image with per-cell reveal animations"
      contains: "grid-cols"
    - path: "src/components/operator/sections/AskIntelligentlyPanel.tsx"
      provides: "Clickable 72-cell grid for operator with reveal tracking"
      contains: "revealedAnimals"
    - path: "src/state/showStore.ts"
      provides: "revealedAnimals array in sectionState"
      contains: "revealedAnimals"
  key_links:
    - from: "src/components/operator/sections/AskIntelligentlyPanel.tsx"
      to: "src/state/showStore.ts"
      via: "setSectionState({ revealedAnimals })"
      pattern: "setSectionState.*revealedAnimals"
    - from: "src/components/audience/sections/AskIntelligentlyDisplay.tsx"
      to: "src/state/showStore.ts"
      via: "useShowStore sectionState.revealedAnimals"
      pattern: "sectionState\\.revealedAnimals"
---

<objective>
Close verification gap: Ask Intelligently dynamic 72-animal grid with per-animal reveal

The verification report found that Ask Intelligently uses a single composite image with no per-animal interaction or reveal state. This plan adds a CSS grid overlay on the existing 3840x2160 `animals.png` composite, enabling the operator to click individual animal cells, track which are revealed, and broadcast reveal state to the audience display with entrance animations.

Purpose: Satisfy SECT-09 requirement and Truth #6 ("Ask Intelligently displays 72 animal photos in a dynamic grid with reveal animations")
Output: Updated Ask Intelligently operator panel and audience display with per-animal grid interaction
</objective>

<execution_context>
@/Users/jweaker/.config/opencode/get-shit-done/workflows/execute-plan.md
@/Users/jweaker/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-quiz-sections/06-05-SUMMARY.md
@src/state/showStore.ts
@src/components/operator/sections/AskIntelligentlyPanel.tsx
@src/components/audience/sections/AskIntelligentlyDisplay.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add revealedAnimals state and upgrade operator grid</name>
  <files>
    src/state/showStore.ts
    src/components/operator/sections/AskIntelligentlyPanel.tsx
  </files>
  <action>
    **showStore.ts changes:**
    1. Add `revealedAnimals: number[]` to the `sectionState` type (line ~105 area). This is an array of cell indices (0-71) that have been revealed/asked about.
    2. Add `revealedAnimals: []` to `defaultSectionState` (line ~131 area). This ensures it resets when jumping sections.

    **AskIntelligentlyPanel.tsx changes:**
    Replace the static thumbnail image in both pre-start and active phases with a clickable 9-column CSS grid overlay on the composite image.

    Grid design (operator panel, compact for mission-control density):
    - 72 cells in a `grid-cols-9` layout (9 columns x 8 rows) — matches the composite image's visual grid arrangement
    - Each cell is a small clickable square. The grid is overlaid on a scaled-down version of `animals.png` using CSS `position: relative` on a container with the image as background
    - Grid container: `aspect-[16/9]` to match the 3840x2160 image ratio, `bg-cover` with `animals.png` as background-image, `rounded`, `border border-border`
    - Each cell: `absolute` positioning via CSS grid overlay, semi-transparent on default, highlighted/dimmed on reveal
    - Clicking a cell during active phase: adds its index to `revealedAnimals` via `setSectionState`, also triggers the existing `handleDeduct` logic (-1 point + askedQuestions increment)
    - Revealed cells show a semi-transparent dark overlay (e.g., `bg-black/60`) to indicate "already asked"
    - Unrevealed cells show a subtle hover effect (`hover:bg-white/20`) for discoverability

    Implementation approach:
    - Container div with `position: relative`, background-image set to `animalsImage`, `bg-cover bg-center`
    - Inside: a CSS grid of 72 divs with `grid-cols-9`, `position: absolute inset-0` overlaying the image
    - Each cell div: `cursor-pointer`, `border border-white/10` for grid lines, `transition-colors`
    - On click (active phase only): check if index already in `revealedAnimals`, if not — push index, call handleDeduct
    - Pre-start phase: show grid read-only (no click handlers), same visual but cells are not interactive
    - Active phase: cells are clickable, revealed cells show dark overlay

    Keep all existing functionality intact: start button (+20 points, 120s timer), Q/E hotkeys, remaining points bar, team display. The grid replaces the static `<img>` thumbnail in both pre-start and active views.

    Note: Import `animalsImage` continues to be used, but now as `url()` in a style prop for background-image rather than an `<img>` tag.
  </action>
  <verify>
    `npx tsc --noEmit` passes. The showStore type includes `revealedAnimals: number[]`. The operator panel renders 72 clickable grid cells.
  </verify>
  <done>
    Operator panel shows a 9x8 grid overlaid on the animals composite image. Clicking a cell during active phase marks it as revealed (dark overlay) and deducts 1 point. State stored in `sectionState.revealedAnimals` and broadcast via BroadcastChannel.
  </done>
</task>

<task type="auto">
  <name>Task 2: Upgrade audience display with animated grid reveal</name>
  <files>
    src/components/audience/sections/AskIntelligentlyDisplay.tsx
  </files>
  <action>
    Replace the static `<img>` audience display with a full-screen grid overlay on the composite image, driven by `sectionState.revealedAnimals` from the store.

    Audience display design:
    - Container: full available space with `animals.png` as background image (same aspect ratio treatment as operator, but larger — fills the broadcast area within safe margins)
    - CSS grid overlay: 72 cells in `grid-cols-9`, absolutely positioned over the image
    - Default state (unrevealed): cells are fully transparent — the animal image shows through clearly
    - Revealed state: cell gets a motion-animated highlight effect. Use `motion.div` with:
      - `initial={{ opacity: 0, scale: 1.2 }}` 
      - `animate={{ opacity: 1, scale: 1 }}`
      - A semi-transparent colored overlay (e.g., `bg-emerald-500/30` with `ring-2 ring-emerald-400`) to highlight which animal was just asked about
      - `transition={{ duration: 0.4, ease: [0.4, 0, 0.2, 1] }}`
    - Use `AnimatePresence` so reveals animate in when they appear

    Read `revealedAnimals` from the store:
    ```
    const revealedAnimals = useShowStore((s) => s.sectionState.revealedAnimals)
    ```

    For each cell (0-71), check if its index is in the `revealedAnimals` array. If yes, render the animated highlight overlay. If no, render nothing (transparent).

    Keep the section title "اسأل بذكاء" and `TimerDisplay` component. The grid replaces the static `<img>` tag. The title can move to a top overlay bar with semi-transparent background for broadcast readability.

    Performance note: `revealedAnimals.includes(index)` is fine for 72 items. No Set conversion needed.
  </action>
  <verify>
    `npx tsc --noEmit` passes. Open the app, navigate to Ask Intelligently section. Audience display shows the animal grid image with transparent overlay. When operator clicks a cell, the corresponding cell on audience display animates in with a highlight effect.
  </verify>
  <done>
    Audience display shows the 72-animal composite image with a CSS grid overlay. Revealed animals display an animated highlight (scale+fade entrance with colored overlay). State syncs from operator via BroadcastChannel — clicking on operator panel immediately highlights the cell on audience display.
  </done>
</task>

</tasks>

<verification>
1. `npx tsc --noEmit` — no type errors
2. `npm run build` — production build succeeds
3. Open operator panel → navigate to Ask Intelligently section
4. Verify 9x8 grid is visible over the animals image on both screens
5. Click Start → click a grid cell → verify:
   - Cell shows dark overlay on operator panel
   - Cell shows animated highlight on audience display
   - Points counter decrements by 1
   - `askedQuestions` increments by 1
6. Click multiple cells → verify each animates independently on audience
7. Navigate away and back → verify `revealedAnimals` resets (sectionState reset on jump)
</verification>

<success_criteria>
- 72-cell grid overlay visible on both operator and audience displays over the composite animals.png
- Operator can click individual cells to mark animals as revealed
- Each reveal syncs to audience display with entrance animation (scale+fade)
- Existing scoring mechanics (start +20, Q key -1, E key end) continue to work
- Grid cells and click-to-reveal work alongside existing point deduction
- Section state resets when navigating away
</success_criteria>

<output>
After completion, create `.planning/phases/06-quiz-sections/06-06-SUMMARY.md`
</output>
