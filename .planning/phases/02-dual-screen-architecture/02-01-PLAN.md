---
phase: 02-dual-screen-architecture
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - src/state/showStore.ts
  - src/state/sync/broadcastMiddleware.ts
  - src/state/operatorStore.ts
  - src/components/ui/*.tsx
  - src/lib/utils.ts
  - components.json
  - tailwind.config.js
autonomous: true

must_haves:
  truths:
    - "State changes in one window appear in other windows within 100ms"
    - "Theme preference persists across page refreshes"
    - "BroadcastChannel connects automatically when multiple windows open"
  artifacts:
    - path: "src/state/sync/broadcastMiddleware.ts"
      provides: "BroadcastChannel middleware for Zustand"
      exports: ["broadcast"]
    - path: "src/state/operatorStore.ts"
      provides: "Operator-only state (theme, panel sizes, safe area)"
      exports: ["useOperatorStore"]
    - path: "src/components/ui/button.tsx"
      provides: "shadcn/ui Button component"
    - path: "src/lib/utils.ts"
      provides: "cn() utility for class merging"
      exports: ["cn"]
  key_links:
    - from: "src/state/showStore.ts"
      to: "src/state/sync/broadcastMiddleware.ts"
      via: "middleware wrapper"
      pattern: "broadcast\\("
---

<objective>
Set up cross-window state synchronization infrastructure and install shadcn/ui component library.

Purpose: Enable real-time state sync between operator and audience windows via BroadcastChannel, and provide production-quality UI components for the operator panel.

Output: BroadcastChannel middleware wrapping showStore, operatorStore for operator-specific state (theme, safe area), and shadcn/ui components ready for use.
</objective>

<execution_context>
@/Users/jweaker/.config/opencode/get-shit-done/workflows/execute-plan.md
@/Users/jweaker/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-dual-screen-architecture/02-RESEARCH.md
@src/state/showStore.ts
@package.json
</context>

<tasks>

<task type="auto">
  <name>Task 1: Initialize shadcn/ui with required components</name>
  <files>
    package.json
    components.json
    tailwind.config.js
    src/lib/utils.ts
    src/components/ui/button.tsx
    src/components/ui/resizable.tsx
    src/components/ui/switch.tsx
    src/components/ui/slider.tsx
    src/components/ui/input.tsx
    src/components/ui/label.tsx
  </files>
  <action>
    Install shadcn/ui with pnpm:
    
    1. Run `pnpm dlx shadcn@latest init` with these options:
       - Style: New York
       - Base color: Neutral
       - CSS variables: yes
       - tailwind.config.js location: leave empty (Tailwind v4)
       - components.json: accept defaults
    
    2. Add required components:
       `pnpm dlx shadcn@latest add button resizable switch slider input label`
    
    3. Create src/lib/utils.ts with cn() utility if not created:
       ```typescript
       import { clsx, type ClassValue } from "clsx"
       import { twMerge } from "tailwind-merge"
       
       export function cn(...inputs: ClassValue[]) {
         return twMerge(clsx(inputs))
       }
       ```
    
    Note: shadcn/ui may create an empty tailwind.config.js - this is expected for Tailwind v4.
    The @tailwindcss/vite plugin handles configuration.
  </action>
  <verify>
    - `pnpm list lucide-react react-resizable-panels clsx tailwind-merge` shows installed
    - src/components/ui/button.tsx exists
    - src/components/ui/resizable.tsx exists
    - src/lib/utils.ts exports cn function
    - `pnpm build` succeeds
  </verify>
  <done>shadcn/ui initialized with Button, Resizable, Switch, Slider, Input, Label components available</done>
</task>

<task type="auto">
  <name>Task 2: Create BroadcastChannel middleware for Zustand</name>
  <files>
    src/state/sync/broadcastMiddleware.ts
    src/state/showStore.ts
  </files>
  <action>
    Create BroadcastChannel middleware that syncs state between windows:
    
    1. Create src/state/sync/broadcastMiddleware.ts:
       - Define middleware type that wraps StateCreator
       - Create BroadcastChannel with configurable channel name
       - On incoming message: use store.setState() directly (bypasses wrapper, prevents echo)
       - On state change: broadcast full state to channel
       - Include message type 'STATE_UPDATE' and 'STATE_REQUEST' (for initial sync)
       - Export broadcast middleware function
    
    2. Modify src/state/showStore.ts:
       - Import broadcast middleware
       - Wrap existing persist middleware with broadcast middleware
       - Use channel name 'quiz-show-state'
       - Order: broadcast(persist(storeCreator)) - broadcast wraps persist
    
    Handle initial sync for late-joining windows:
    - On 'STATE_REQUEST' message, respond with current state
    - New windows send STATE_REQUEST on mount to get current state
    
    Avoid echo loops by checking if incoming state equals current state before applying.
  </action>
  <verify>
    - Open two browser tabs at localhost:5173
    - In dev tools console of Tab A: `useShowStore.getState().setRightScore(42)`
    - Tab B console: `useShowStore.getState().rightScore` returns 42
    - Changes propagate within 100ms (no visible delay)
    - `pnpm build` succeeds
  </verify>
  <done>State changes in showStore sync automatically between all open windows via BroadcastChannel</done>
</task>

<task type="auto">
  <name>Task 3: Create operatorStore for operator-specific state</name>
  <files>
    src/state/operatorStore.ts
    src/state/index.ts
  </files>
  <action>
    Create operator-only store for settings that don't need cross-window sync:
    
    1. Create src/state/operatorStore.ts:
       - Define SafeArea interface: { top, right, bottom, left: number; unit: 'px' | '%' }
       - Define OperatorState interface:
         - theme: 'light' | 'dark' | 'system'
         - safeArea: SafeArea
         - confidenceMonitorSize: number (percentage, default 30)
         - audienceWindowConnected: boolean
       - Default safe area: { top: 0, right: 0, bottom: 15, left: 0, unit: '%' }
       - Actions: setTheme, setSafeArea, setConfidenceMonitorSize, setAudienceWindowConnected
       - Use persist middleware with key 'operator-storage'
       - Do NOT use broadcast middleware (operator state is local only)
    
    2. Update src/state/index.ts:
       - Export useOperatorStore
       - Export SafeArea type
    
    Note: audienceWindowConnected is set by window lifecycle manager (Plan 03), not persisted.
  </action>
  <verify>
    - `pnpm build` succeeds
    - In dev console: `useOperatorStore.getState().safeArea` returns default values
    - Set theme, refresh page, theme persists
    - Opening second tab does NOT sync operatorStore (it's local only)
  </verify>
  <done>operatorStore created with theme, safe area, and panel size settings that persist locally</done>
</task>

</tasks>

<verification>
1. `pnpm build` completes without errors
2. shadcn/ui components render correctly (Button visible, Resizable works)
3. BroadcastChannel syncs showStore state between tabs within 100ms
4. operatorStore persists locally but does not sync between tabs
5. Theme preference survives page refresh
</verification>

<success_criteria>
- shadcn/ui installed with Button, Resizable, Switch, Slider, Input, Label
- BroadcastChannel middleware syncs showStore across windows
- operatorStore holds operator-specific settings (theme, safe area)
- No TypeScript errors, build succeeds
</success_criteria>

<output>
After completion, create `.planning/phases/02-dual-screen-architecture/02-01-SUMMARY.md`
</output>
