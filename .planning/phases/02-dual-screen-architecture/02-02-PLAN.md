---
phase: 02-dual-screen-architecture
plan: 02
type: execute
wave: 2
depends_on: ["02-01"]
files_modified:
  - src/App.tsx
  - src/app/OperatorRoot.tsx
  - src/app/AudienceRoot.tsx
  - src/screens/operator/OperatorPanel.tsx
  - src/screens/operator/Settings.tsx
  - src/screens/audience/AudienceDisplay.tsx
  - src/components/operator/ThemeProvider.tsx
  - src/components/operator/ThemeToggle.tsx
autonomous: true

must_haves:
  truths:
    - "Navigating to /operator shows operator panel with controls"
    - "Navigating to /audience shows broadcast-ready audience display"
    - "Safe area margins visibly affect content positioning on audience display"
    - "Theme toggle switches between light and dark mode on operator panel"
  artifacts:
    - path: "src/screens/operator/OperatorPanel.tsx"
      provides: "Main operator view with resizable panel layout"
      min_lines: 50
    - path: "src/screens/audience/AudienceDisplay.tsx"
      provides: "Audience-facing display with safe area support"
      min_lines: 40
    - path: "src/screens/operator/Settings.tsx"
      provides: "Safe area configuration UI"
      exports: ["Settings"]
    - path: "src/app/OperatorRoot.tsx"
      provides: "Operator wrapper with theme provider"
    - path: "src/app/AudienceRoot.tsx"
      provides: "Audience wrapper with fullscreen styling"
  key_links:
    - from: "src/App.tsx"
      to: "src/app/OperatorRoot.tsx"
      via: "Route component"
      pattern: "path.*operator"
    - from: "src/screens/audience/AudienceDisplay.tsx"
      to: "src/state/operatorStore.ts"
      via: "useOperatorStore selector"
      pattern: "useOperatorStore.*safeArea"
---

<objective>
Create the operator and audience routes with their respective layouts and components.

Purpose: Establish the dual-screen architecture with operator panel at `/operator` and audience display at `/audience`. Operator controls the show from the panel; audience sees polished broadcast output.

Output: Working `/operator` and `/audience` routes, operator panel with theme toggle, audience display with configurable safe area, and settings page for safe area configuration.
</objective>

<execution_context>
@/Users/jweaker/.config/opencode/get-shit-done/workflows/execute-plan.md
@/Users/jweaker/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-dual-screen-architecture/02-RESEARCH.md
@.planning/phases/02-dual-screen-architecture/02-01-SUMMARY.md
@src/App.tsx
@src/state/operatorStore.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create operator and audience root wrappers with routing</name>
  <files>
    src/app/OperatorRoot.tsx
    src/app/AudienceRoot.tsx
    src/components/operator/ThemeProvider.tsx
    src/components/operator/ThemeToggle.tsx
    src/App.tsx
  </files>
  <action>
    Create wrapper components and update routing:
    
    1. Create src/components/operator/ThemeProvider.tsx:
       - Follow shadcn/ui Vite dark mode pattern from research
       - Read theme from operatorStore, sync to document.documentElement classList
       - Handle 'system' preference with matchMedia listener
       - Prevent FOUC by applying theme class synchronously in useLayoutEffect
    
    2. Create src/components/operator/ThemeToggle.tsx:
       - Use shadcn/ui Switch component
       - Toggle between light/dark (skip system for simplicity)
       - Read/write via useOperatorStore
       - Include sun/moon icons from lucide-react
    
    3. Create src/app/OperatorRoot.tsx:
       - Wrap children in ThemeProvider
       - Apply operator-specific base styles (bg-background text-foreground)
       - Include OperatorErrorBoundary
       - Export as default
    
    4. Create src/app/AudienceRoot.tsx:
       - Fullscreen container with broadcast background (radial gradient, blues)
       - No theme toggle (always uses broadcast styling)
       - Include AudienceErrorBoundary
       - Export as default
    
    5. Update src/App.tsx:
       - Add routes: `/operator/*` renders OperatorRoot with nested routes
       - Add routes: `/audience` renders AudienceRoot with AudienceDisplay
       - Keep existing routes at root (legacy support during migration)
       - Root "/" redirects to "/operator" (operator is the main entry point)
    
    RTL note: Both roots inherit dir="rtl" from html element set in Phase 1.
  </action>
  <verify>
    - Navigate to localhost:5173/operator - shows operator styling
    - Navigate to localhost:5173/audience - shows audience styling (blue gradient)
    - Navigate to localhost:5173/ - redirects to /operator
    - Toggle theme on operator - page switches between light/dark
    - `pnpm build` succeeds
  </verify>
  <done>Operator and audience routes exist with appropriate wrappers and theme support</done>
</task>

<task type="auto">
  <name>Task 2: Build operator panel layout with resizable sections</name>
  <files>
    src/screens/operator/OperatorPanel.tsx
    src/screens/operator/OperatorControls.tsx
  </files>
  <action>
    Create the main operator panel layout:
    
    1. Create src/screens/operator/OperatorPanel.tsx:
       - Use ResizablePanelGroup from shadcn/ui (horizontal orientation)
       - Left panel (70% default, min 40%): main controls area
       - Right panel (30% default, min 15%): confidence monitor placeholder
       - ResizableHandle with visible handle between panels
       - Store panel size in operatorStore.confidenceMonitorSize
       - Full height layout (h-screen)
    
    2. Create src/screens/operator/OperatorControls.tsx:
       - Placeholder for now - will contain actual show controls in later phases
       - Show current state from showStore (scores, team names, current turn)
       - Include ThemeToggle in header area
       - Add "Open Audience Window" button (functionality in Plan 03)
       - Responsive padding and layout
    
    Layout structure:
    ```
    ┌─────────────────────────────────┬──────────────┐
    │  Header: Theme Toggle + Title   │              │
    ├─────────────────────────────────│  Confidence  │
    │  Main Controls Area             │  Monitor     │
    │  (scores, state, buttons)       │  (Plan 03)   │
    │                                 │              │
    └─────────────────────────────────┴──────────────┘
    ```
    
    Use Tailwind classes for layout. Support both keyboard and mouse resizing.
  </action>
  <verify>
    - /operator shows resizable two-panel layout
    - Drag handle resizes panels
    - Panel sizes persist across refresh (stored in operatorStore)
    - Scores from showStore display correctly
    - ThemeToggle visible and functional
    - `pnpm build` succeeds
  </verify>
  <done>Operator panel has resizable layout with controls area and confidence monitor placeholder</done>
</task>

<task type="auto">
  <name>Task 3: Create audience display with safe area and settings page</name>
  <files>
    src/screens/audience/AudienceDisplay.tsx
    src/screens/operator/Settings.tsx
    src/lib/safeArea.ts
  </files>
  <action>
    Build the audience display and safe area configuration:
    
    1. Create src/lib/safeArea.ts:
       - getContentStyle(safeArea: SafeArea): CSSProperties function
       - Converts safe area margins to absolute positioning
       - Handles both px and % units
    
    2. Create src/screens/audience/AudienceDisplay.tsx:
       - Fixed dimensions: 3840x2160 (4K) with CSS aspect-ratio: 16/9
       - Container fills viewport, content scales to fit (CSS contain: layout)
       - Outer container: broadcast background (the gradient from current App.tsx)
       - Inner content area: positioned by safe area margins
       - Read safe area from operatorStore
       - Display current show state (scores, team names) - placeholder content
       - Use ResizeObserver to handle viewport changes gracefully
    
    3. Create src/screens/operator/Settings.tsx:
       - Four number inputs for safe area margins (top, right, bottom, left)
       - Unit toggle: px or %
       - Slider alternative for percentage mode (0-50% range per edge)
       - Live preview text showing current values
       - Save updates to operatorStore immediately (no submit button)
       - Back navigation to main operator panel
    
    4. Add /operator/settings route in App.tsx nested under OperatorRoot
    
    Safe area visual: Content stays within margins, background fills full 4K canvas.
  </action>
  <verify>
    - /audience shows 4K-proportioned display with blue gradient background
    - Content respects safe area margins (visible when margins set > 0)
    - /operator/settings shows margin inputs
    - Changing margins in settings immediately affects audience display (via BroadcastChannel sync)
    - Margins persist across page refresh
    - `pnpm build` succeeds
  </verify>
  <done>Audience display renders with configurable safe area; settings page allows margin adjustment</done>
</task>

</tasks>

<verification>
1. `pnpm build` completes without errors
2. /operator route shows themed operator panel with resizable layout
3. /audience route shows broadcast-ready display with safe area
4. Theme toggle works and persists
5. Safe area settings change audience display layout
6. Scores from showStore appear on both operator and audience views
</verification>

<success_criteria>
- /operator and /audience routes functional
- Operator panel has resizable two-column layout
- Audience display maintains 16:9 aspect ratio with safe area margins
- Theme toggle persists preference
- Safe area configurable via settings page
</success_criteria>

<output>
After completion, create `.planning/phases/02-dual-screen-architecture/02-02-SUMMARY.md`
</output>
