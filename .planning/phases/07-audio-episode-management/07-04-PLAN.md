---
phase: 07-audio-episode-management
plan: 04
type: execute
wave: 2
depends_on: ["07-03"]
files_modified:
  - src/App.tsx
  - src/screens/editor/EpisodeEditor.tsx
  - src/screens/editor/components/EditorHeader.tsx
  - src/screens/editor/components/MetadataSection.tsx
  - src/screens/editor/components/QuestionListEditor.tsx
  - src/screens/editor/components/BulkPasteDialog.tsx
  - src/screens/editor/components/SpeedQuestionsForm.tsx
  - src/screens/editor/components/WindowsForm.tsx
  - src/screens/editor/components/PuzzleForm.tsx
  - src/screens/editor/components/DebateForm.tsx
  - src/screens/editor/components/RapidQuestionsForm.tsx
  - src/screens/editor/components/AudienceQuestionsForm.tsx
  - src/screens/editor/components/ValidationSummary.tsx
  - src/lib/pasteParser.ts
  - src/screens/operator/OperatorPanel.tsx
autonomous: true

must_haves:
  truths:
    - "Operator can create a new episode from scratch via /editor route"
    - "Operator can edit existing episode questions, answers, timers, and team names"
    - "Editor validates data in real-time with inline error messages"
    - "Episode can be imported from JSON file and exported after editing"
    - "Operator can load an episode file from the operator panel during show"
    - "Editor supports bulk paste with auto-format detection"
    - "Editor offers three starting points: blank, template, clone from file"
  artifacts:
    - path: "src/screens/editor/EpisodeEditor.tsx"
      provides: "Main editor page with all sections, state management, validation"
      min_lines: 100
    - path: "src/screens/editor/components/EditorHeader.tsx"
      provides: "Header with new/import/export buttons and validation summary"
    - path: "src/screens/editor/components/QuestionListEditor.tsx"
      provides: "Reusable question list with add/remove and bulk paste trigger"
    - path: "src/screens/editor/components/BulkPasteDialog.tsx"
      provides: "Dialog for pasting questions with format auto-detection and preview"
    - path: "src/lib/pasteParser.ts"
      provides: "Parse pasted text into Question[] with format detection"
      exports: ["parsePastedQuestions", "detectPasteFormat"]
    - path: "src/screens/editor/components/WindowsForm.tsx"
      provides: "Windows of Knowledge 5-category editor with 2 questions each"
    - path: "src/App.tsx"
      provides: "/editor route added with lazy loading"
      contains: "editor"
    - path: "src/screens/operator/OperatorPanel.tsx"
      provides: "Episode file picker button for loading episodes during show"
  key_links:
    - from: "src/screens/editor/EpisodeEditor.tsx"
      to: "src/lib/episodeSchema.ts"
      via: "validates episode state with EpisodeSchema.safeParse"
      pattern: "EpisodeSchema\\.safeParse\\|validateEpisode"
    - from: "src/screens/editor/EpisodeEditor.tsx"
      to: "src/lib/episodeSchema.ts"
      via: "creates episodes with factory functions"
      pattern: "createBlankEpisode\\|createTemplateEpisode"
    - from: "src/screens/editor/components/BulkPasteDialog.tsx"
      to: "src/lib/pasteParser.ts"
      via: "parses pasted text into questions"
      pattern: "parsePastedQuestions"
    - from: "src/screens/operator/OperatorPanel.tsx"
      to: "src/lib/episodeSchema.ts"
      via: "validates imported episode file"
      pattern: "validateEpisode\\|EpisodeSchema"
    - from: "src/App.tsx"
      to: "src/screens/editor/EpisodeEditor.tsx"
      via: "lazy loaded route"
      pattern: "lazy.*import.*editor"
---

<objective>
Build the complete episode editor at /editor route with all section forms, bulk paste, import/export, validation, and add episode file picker to operator panel.

Purpose: Enables the operator to prepare episodes before shows — create from scratch, edit questions/answers, import existing JSON files, validate completeness, and export for reuse. The operator panel gets a file picker to load/swap episodes during live show.
Output: /editor route with full CRUD episode editing, paste parser, file import/export, operator panel file picker
</objective>

<execution_context>
@/Users/jweaker/.config/opencode/get-shit-done/workflows/execute-plan.md
@/Users/jweaker/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-audio-episode-management/07-RESEARCH.md
@.planning/phases/07-audio-episode-management/07-03-SUMMARY.md
@src/lib/episodeSchema.ts
@src/state/showStore.ts
@src/App.tsx
@src/screens/operator/OperatorPanel.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Paste parser, editor route, header, metadata, and reusable question list</name>
  <files>src/lib/pasteParser.ts, src/App.tsx, src/screens/editor/EpisodeEditor.tsx, src/screens/editor/components/EditorHeader.tsx, src/screens/editor/components/MetadataSection.tsx, src/screens/editor/components/QuestionListEditor.tsx, src/screens/editor/components/BulkPasteDialog.tsx, src/screens/editor/components/ValidationSummary.tsx</files>
  <action>
1. **Create `src/lib/pasteParser.ts`** — paste format detection and parsing:
   - `detectPasteFormat(text: string): 'json' | 'tsv' | 'qa-pairs' | 'lines'`
     - If text starts with `{` or `[`: try JSON.parse → 'json'
     - If most lines contain `\t`: 'tsv'
     - If lines match Q/A patterns (Q: / A: or س: / ج: or numbered with indented answers): 'qa-pairs'
     - Fallback: 'lines'
   - `parsePastedQuestions(text: string): Question[]`
     - Detect format, then parse accordingly:
     - JSON: parse, extract if it's an array of objects with text/answer fields
     - TSV: split lines by tab, first column = text, second = answer, third = duration (optional), fourth = marks (optional)
     - QA pairs: match patterns, pair up question + answer lines
     - Lines: each non-empty line becomes a question with empty answer
     - Return Question[] using QuestionSchema defaults for missing fields
   - Handle Arabic question marks `؟` and Arabic-Indic numerals `١٢٣` in detection

2. **Add /editor route to `src/App.tsx`:**
   - Add lazy import: `const EpisodeEditor = lazy(() => import('./screens/editor/EpisodeEditor'))`
   - Add route: `<Route path="/editor" element={<EpisodeEditor />} />` — top-level route, not nested under operator or audience
   - Place it after the `/operator` route block and before `/audience`

3. **Create `src/screens/editor/EpisodeEditor.tsx`** — main editor page:
   - React state: `episode` (Episode type from Zod), `errors` (Record<string, string[]>)
   - Initialize with `createBlankEpisode()` from episodeSchema
   - On any change: debounced validation (300ms) via `validateEpisode(episode)` → update errors state
   - Single scrollable page layout with vertical sections
   - Render in order: EditorHeader → MetadataSection → SpeedQuestionsForm → WindowsForm → PuzzleForm → DebateForm → RapidQuestionsForm → AudienceQuestionsForm → ValidationSummary at bottom
   - Pass section slice + updater callback to each form component
   - Helper: `updateParts(key, value)` that creates new episode with updated parts[key]
   - Import/export handlers defined here (lifted up):
     - `handleImport(file: File)`: FileReader → JSON.parse → validateEpisode → if valid set episode state, else show errors
     - `handleExport()`: JSON.stringify(episode, null, 2) → Blob → download as `{title || 'episode'}.json`
     - `handleNew(type: 'blank' | 'template' | 'clone')`: blank → createBlankEpisode(), template → createTemplateEpisode(), clone → open file picker then handleImport
   - Style: dark background (consistent with operator theme), max-w-4xl mx-auto with py-8 spacing, each section as a card with border/shadow

4. **Create `src/screens/editor/components/EditorHeader.tsx`:**
   - Shows episode title (editable inline) or "حلقة جديدة" (New Episode) placeholder
   - Three action buttons (per user decision — three starting points):
     - "جديد" (New/Blank) — calls handleNew('blank')
     - "قالب" (Template) — calls handleNew('template')
     - "استنساخ" (Clone) — opens file picker, calls handleNew('clone')
   - Import button: "استيراد" — opens file picker input[type=file accept=.json]
   - Export button: "تصدير" — calls handleExport()
   - Link back to /operator: "العودة للتحكم" with left arrow icon

5. **Create `src/screens/editor/components/MetadataSection.tsx`:**
   - Episode title input (optional)
   - Left team name input (required — show error if empty)
   - Right team name input (required — show error if empty)
   - Date input (optional, type="date")
   - Settings section (collapsible): puzzleDuration, debateDuration, rapidQuestionsDuration, poeticChaseDuration, askIntelligentlyDuration — all number inputs with labels in Arabic
   - Inline validation errors shown below each field (red text)
   - Props: `metadata` (title, date, teamNames, settings), `onChange`, `errors`

6. **Create `src/screens/editor/components/QuestionListEditor.tsx`** — reusable component:
   - Renders a list of Question items with editable fields per row: text (textarea), answer (input), duration (number), marks (number)
   - Add button at bottom to append a new blank question
   - Remove button (X icon) per row to delete a question
   - "لصق مجمّع" (Bulk Paste) button opens BulkPasteDialog
   - Props: `questions: Question[]`, `onChange: (questions: Question[]) => void`, `errors?: Record<string, string[]>`, `showMarks?: boolean` (some sections don't use marks), `showDuration?: boolean`
   - Each field shows inline error if present in errors prop

7. **Create `src/screens/editor/components/BulkPasteDialog.tsx`:**
   - shadcn/ui Dialog component
   - Large textarea for pasting
   - On paste/change: calls `parsePastedQuestions()` and shows preview of parsed result
   - Preview shows: "تم اكتشاف X سؤال" (Detected X questions) + format type + first 3 questions preview
   - "إضافة" (Add) button: appends parsed questions to existing list and closes dialog
   - "استبدال" (Replace) button: replaces all questions with parsed ones
   - "إلغاء" (Cancel) button: closes without action

8. **Create `src/screens/editor/components/ValidationSummary.tsx`:**
   - Shows overall validation status: ✅ "جاهز" (Ready) or ⚠️ "X أخطاء" (X errors)
   - Lists all validation errors grouped by section
   - Each error is clickable (scrolls to the relevant section — use id attributes on section cards)
   - Only shows when there are errors (hidden when valid)
  </action>
  <verify>
Run `npx tsc --noEmit` — zero type errors. Verify /editor route exists in App.tsx. Verify EpisodeEditor.tsx imports and renders all section form components (even if some are stubs at this point — SpeedQuestionsForm at minimum should be functional). Verify pasteParser.ts exports parsePastedQuestions and detectPasteFormat.
  </verify>
  <done>Editor route accessible at /editor. Header with new/import/export/clone. Metadata editing works. QuestionListEditor reusable with bulk paste. Validation summary shows errors. Paste parser detects JSON/TSV/QA/lines formats.</done>
</task>

<task type="auto">
  <name>Task 2: Section-specific form components and operator panel file picker</name>
  <files>src/screens/editor/components/SpeedQuestionsForm.tsx, src/screens/editor/components/WindowsForm.tsx, src/screens/editor/components/PuzzleForm.tsx, src/screens/editor/components/DebateForm.tsx, src/screens/editor/components/RapidQuestionsForm.tsx, src/screens/editor/components/AudienceQuestionsForm.tsx, src/screens/operator/OperatorPanel.tsx</files>
  <action>
1. **Create `src/screens/editor/components/SpeedQuestionsForm.tsx`:**
   - Section card with title "سؤال السرعة" (Speed Question)
   - Uses QuestionListEditor with `showMarks={false}` (speed questions don't use marks)
   - Props: `questions: Question[]`, `onChange: (q: Question[]) => void`, `errors`
   - Section id attribute for scroll-to: `id="section-speed-questions"`

2. **Create `src/screens/editor/components/WindowsForm.tsx`:**
   - Section card with title "نوافذ المعرفة" (Windows of Knowledge)
   - 5 category sub-sections, each with Arabic label:
     - العلوم الطبيعية (naturalSciences)
     - العلوم الإنسانية (humanSciences)
     - المتفرقات (misc)
     - الفنون (arts)
     - الدين (religion)
   - Each category: 1-2 questions using QuestionListEditor with `showMarks={true}` and max 2 questions enforced (disable add button when at 2)
   - Props: `windows: WindowsData`, `onChange: (w: WindowsData) => void`, `errors`
   - Section id: `id="section-windows"`

3. **Create `src/screens/editor/components/PuzzleForm.tsx`:**
   - Section card with title "الألغاز" (Puzzle)
   - Uses QuestionListEditor with `showDuration={true}` and `showMarks={false}`
   - Additional setting: puzzle duration override (number input, inherits from episode settings as default)
   - Props: `puzzles: Question[]`, `onChange: (p: Question[]) => void`, `errors`
   - Section id: `id="section-puzzles"`

4. **Create `src/screens/editor/components/DebateForm.tsx`:**
   - Section card with title "النقاش" (Debate)
   - Single question — NOT a list. Shows:
     - Topic text (textarea for debate topic)
     - Duration input (defaults to episode settings debateDuration)
   - Props: `debate: Question`, `onChange: (d: Question) => void`, `errors`
   - Section id: `id="section-debate"`

5. **Create `src/screens/editor/components/RapidQuestionsForm.tsx`:**
   - Section card with title "الرشق السريع" (Rapid Questions)
   - Title input for the question set
   - Uses QuestionListEditor for the questions array with `showMarks={false}`
   - Note: This edits quickQuestions[0] (the first quick question set used by rapid questions)
   - Props: `quickQuestions: QuickQuestionSet[]`, `onChange: (q: QuickQuestionSet[]) => void`, `errors`
   - Section id: `id="section-rapid-questions"`

6. **Create `src/screens/editor/components/AudienceQuestionsForm.tsx`:**
   - Section card with title "أسئلة الجمهور" (Audience Questions)
   - Uses QuestionListEditor with `showMarks={false}`
   - Note at top: "هذه الأسئلة تُعرض بين الأقسام الرئيسية" (These questions are shown between main sections)
   - Props: `questions: Question[]`, `onChange: (q: Question[]) => void`, `errors`
   - Section id: `id="section-audience-questions"`

7. **Add episode file picker to `src/screens/operator/OperatorPanel.tsx`:**
   - Add a "تحميل حلقة" (Load Episode) button in the header bar (next to WindowLauncher)
   - Uses hidden `<input type="file" accept=".json" />` triggered by button click
   - On file select: FileReader → JSON.parse → validateEpisode → if valid, call `useShowStore.getState().setData(parsed)` to load into the live show
   - If invalid: show toast/alert with error summary (use window.alert for simplicity, or a small error banner)
   - Button icon: Lucide `FileUp` or `Upload`
   - This lets the operator load/swap episodes during a live show without going to /editor

**All section form components share this pattern:**
- Wrapped in a Card-like div with border, rounded corners, padding
- Section title as h2 with bold font
- Form fields use shadcn/ui Input, Textarea, Label components
- Inline error display: red text below fields when errors[fieldPath] exists
- Consistent spacing: space-y-4 between fields, space-y-6 between sections
  </action>
  <verify>
Run `npx tsc --noEmit` — zero type errors. Run `npm run build` — succeeds. Verify all 6 section form components exist and are imported in EpisodeEditor.tsx. Verify OperatorPanel.tsx has file picker button and input[type=file]. Verify the /editor route renders a scrollable page with all sections visible.
  </verify>
  <done>All 6 section form components render with appropriate fields. Windows has 5 categories. Debate is single-question. Operator panel has file picker for live episode loading. Full editor is functional: create, edit, import, export, validate.</done>
</task>

</tasks>

<verification>
1. `npx tsc --noEmit` passes
2. `npm run build` succeeds
3. /editor route loads and shows all sections
4. Can create blank episode, template episode, or clone from file
5. Can edit team names, questions, answers, durations
6. Validation errors show inline and in summary
7. Bulk paste detects formats and previews parsed questions
8. Export downloads valid JSON file
9. Import validates and loads JSON file
10. Operator panel file picker loads episode into live show
</verification>

<success_criteria>
- /editor route with lazy-loaded EpisodeEditor
- EditorHeader with new/template/clone/import/export buttons
- MetadataSection for title, teams, date, settings
- 6 section-specific form components with adaptive fields
- QuestionListEditor reusable with add/remove/bulk-paste
- BulkPasteDialog with format detection and preview
- ValidationSummary with error grouping and scroll-to
- pasteParser with JSON/TSV/QA/lines detection
- Operator panel file picker for live episode loading
- All real-time validation via Zod schema
- TypeScript compiles, build succeeds
</success_criteria>

<output>
After completion, create `.planning/phases/07-audio-episode-management/07-04-SUMMARY.md`
</output>
