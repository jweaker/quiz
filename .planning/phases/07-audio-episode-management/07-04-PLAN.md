---
phase: 07-audio-episode-management
plan: 04
type: execute
wave: 2
depends_on: ["07-03"]
files_modified:
  - src/App.tsx
  - src/screens/editor/EpisodeEditor.tsx
  - src/screens/editor/components/EditorHeader.tsx
  - src/screens/editor/components/MetadataSection.tsx
  - src/screens/editor/components/ValidationSummary.tsx
autonomous: true

must_haves:
  truths:
    - "Operator can navigate to /editor route"
    - "Editor shell renders with header, metadata section, and validation summary"
    - "Three starting points work: blank, template, clone from file"
    - "Episode import/export from header buttons work"
    - "Real-time validation fires on changes and shows inline errors"
    - "Metadata fields (title, teams, date, settings) are editable"
  artifacts:
    - path: "src/App.tsx"
      provides: "/editor route added with lazy loading"
      contains: "editor"
    - path: "src/screens/editor/EpisodeEditor.tsx"
      provides: "Main editor page with state management, validation, import/export, section placeholders"
      min_lines: 80
    - path: "src/screens/editor/components/EditorHeader.tsx"
      provides: "Header with new/template/clone/import/export buttons"
    - path: "src/screens/editor/components/MetadataSection.tsx"
      provides: "Title, team names, date, settings form fields with inline validation"
    - path: "src/screens/editor/components/ValidationSummary.tsx"
      provides: "Error summary panel with scroll-to-section links"
  key_links:
    - from: "src/screens/editor/EpisodeEditor.tsx"
      to: "src/lib/episodeSchema.ts"
      via: "validates episode state with EpisodeSchema.safeParse"
      pattern: "EpisodeSchema\\.safeParse\\|validateEpisode"
    - from: "src/screens/editor/EpisodeEditor.tsx"
      to: "src/lib/episodeSchema.ts"
      via: "creates episodes with factory functions"
      pattern: "createBlankEpisode\\|createTemplateEpisode"
    - from: "src/App.tsx"
      to: "src/screens/editor/EpisodeEditor.tsx"
      via: "lazy loaded route"
      pattern: "lazy.*import.*editor"
---

<objective>
Create the editor route, main EpisodeEditor shell, header with new/import/export actions, metadata form, and validation summary.

Purpose: Establishes the editor foundation — routing, state management, validation loop, import/export, and the metadata/settings section. Form components for individual sections are added in subsequent plans (07-05, 07-06).
Output: /editor route, EpisodeEditor shell, EditorHeader, MetadataSection, ValidationSummary
</objective>

<execution_context>
@/Users/jweaker/.config/opencode/get-shit-done/workflows/execute-plan.md
@/Users/jweaker/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-audio-episode-management/07-RESEARCH.md
@.planning/phases/07-audio-episode-management/07-03-SUMMARY.md
@src/lib/episodeSchema.ts
@src/state/showStore.ts
@src/App.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Editor route and EpisodeEditor shell with state management</name>
  <files>src/App.tsx, src/screens/editor/EpisodeEditor.tsx</files>
  <action>
1. **Add /editor route to `src/App.tsx`:**
   - Add lazy import: `const EpisodeEditor = lazy(() => import('./screens/editor/EpisodeEditor'))`
   - Add route: `<Route path="/editor" element={<EpisodeEditor />} />` — top-level route, not nested under operator or audience
   - Place it after the `/operator` route block and before `/audience`

2. **Create `src/screens/editor/EpisodeEditor.tsx`** — main editor page:
   - React state: `episode` (Episode type from Zod), `errors` (Record<string, string[]>)
   - Initialize with `createBlankEpisode()` from episodeSchema
   - On any change: debounced validation (300ms) via `validateEpisode(episode)` → update errors state
   - Single scrollable page layout with vertical sections
   - Render in order: EditorHeader → MetadataSection → (placeholder divs for section forms that will be added in 07-05/07-06) → ValidationSummary at bottom
   - Pass section slice + updater callback to each form component
   - Helper: `updateParts(key, value)` that creates new episode with updated parts[key]
   - Import/export handlers defined here (lifted up):
     - `handleImport(file: File)`: FileReader → JSON.parse → validateEpisode → if valid set episode state, else show errors
     - `handleExport()`: JSON.stringify(episode, null, 2) → Blob → download as `{title || 'episode'}.json`
     - `handleNew(type: 'blank' | 'template' | 'clone')`: blank → createBlankEpisode(), template → createTemplateEpisode(), clone → open file picker then handleImport
   - Style: dark background (consistent with operator theme), max-w-4xl mx-auto with py-8 spacing, each section as a card with border/shadow
   - Export default for lazy loading

   **Section form placeholders:** For the section areas (speed, windows, puzzle, debate, rapid, audience), render placeholder card divs with section titles so the page structure is visible. These will be replaced by real form components in 07-05 and 07-06. Example: `<div id="section-speed-questions" className="border rounded-lg p-6"><h2>سؤال السرعة</h2><p className="text-muted-foreground">Coming in next plan</p></div>`
  </action>
  <verify>
Run `npx tsc --noEmit` — zero type errors. Verify /editor route exists in App.tsx. Verify EpisodeEditor.tsx renders EditorHeader, MetadataSection, section placeholders, and ValidationSummary. Verify import/export handlers are defined.
  </verify>
  <done>Editor route accessible at /editor. EpisodeEditor shell renders with state management, debounced validation, import/export handlers, and placeholder sections.</done>
</task>

<task type="auto">
  <name>Task 2: EditorHeader, MetadataSection, and ValidationSummary components</name>
  <files>src/screens/editor/components/EditorHeader.tsx, src/screens/editor/components/MetadataSection.tsx, src/screens/editor/components/ValidationSummary.tsx</files>
  <action>
1. **Create `src/screens/editor/components/EditorHeader.tsx`:**
   - Shows episode title (editable inline) or "حلقة جديدة" (New Episode) placeholder
   - Three action buttons (per user decision — three starting points):
     - "جديد" (New/Blank) — calls handleNew('blank')
     - "قالب" (Template) — calls handleNew('template')
     - "استنساخ" (Clone) — opens file picker, calls handleNew('clone')
   - Import button: "استيراد" — opens file picker input[type=file accept=.json]
   - Export button: "تصدير" — calls handleExport()
   - Link back to /operator: "العودة للتحكم" with left arrow icon
   - Props: `title`, `onTitleChange`, `onNew`, `onImport`, `onExport`

2. **Create `src/screens/editor/components/MetadataSection.tsx`:**
   - Episode title input (optional)
   - Left team name input (required — show error if empty)
   - Right team name input (required — show error if empty)
   - Date input (optional, type="date")
   - Settings section (collapsible): puzzleDuration, debateDuration, rapidQuestionsDuration, poeticChaseDuration, askIntelligentlyDuration — all number inputs with labels in Arabic
   - Inline validation errors shown below each field (red text)
   - Props: `metadata` (title, date, teamNames, settings), `onChange`, `errors`
   - Section id: `id="section-metadata"`

3. **Create `src/screens/editor/components/ValidationSummary.tsx`:**
   - Shows overall validation status: ✅ "جاهز" (Ready) or ⚠️ "X أخطاء" (X errors)
   - Lists all validation errors grouped by section
   - Each error is clickable (scrolls to the relevant section — use id attributes on section cards)
   - Only shows when there are errors (hidden when valid)
   - Props: `errors: Record<string, string[]>`
  </action>
  <verify>
Run `npx tsc --noEmit` — zero type errors. Verify EditorHeader renders new/template/clone/import/export buttons. Verify MetadataSection renders team name inputs with inline error display. Verify ValidationSummary groups errors by section.
  </verify>
  <done>EditorHeader with three starting points + import/export. MetadataSection with team names, date, settings. ValidationSummary with grouped errors and scroll-to links.</done>
</task>

</tasks>

<verification>
1. `npx tsc --noEmit` passes
2. `npm run build` succeeds
3. /editor route loads and shows header, metadata, placeholder sections, and validation summary
4. Can create blank episode, template episode, or clone from file
5. Can edit team names and episode settings
6. Validation errors show inline in metadata and in summary
7. Export downloads valid JSON file
8. Import validates and loads JSON file
</verification>

<success_criteria>
- /editor route with lazy-loaded EpisodeEditor
- EditorHeader with new/template/clone/import/export buttons
- MetadataSection for title, teams, date, settings with inline validation
- ValidationSummary with error grouping and scroll-to
- Section placeholder cards visible for upcoming form components
- TypeScript compiles, build succeeds
</success_criteria>

<output>
After completion, create `.planning/phases/07-audio-episode-management/07-04-SUMMARY.md`
</output>
