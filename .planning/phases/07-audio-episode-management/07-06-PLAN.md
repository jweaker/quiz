---
phase: 07-audio-episode-management
plan: 06
type: execute
wave: 4
depends_on: ["07-05"]
files_modified:
  - src/screens/editor/components/DebateForm.tsx
  - src/screens/editor/components/RapidQuestionsForm.tsx
  - src/screens/editor/components/AudienceQuestionsForm.tsx
  - src/screens/editor/EpisodeEditor.tsx
  - src/screens/operator/OperatorPanel.tsx
autonomous: true

must_haves:
  truths:
    - "Debate section renders as single-question form with duration"
    - "Rapid questions section renders with title and question list"
    - "Audience questions section renders with question list"
    - "All editor sections are complete — no placeholder divs remain"
    - "Operator can load an episode file from the operator panel during show"
  artifacts:
    - path: "src/screens/editor/components/DebateForm.tsx"
      provides: "Debate section single-question form"
    - path: "src/screens/editor/components/RapidQuestionsForm.tsx"
      provides: "Rapid questions section with title + question list"
    - path: "src/screens/editor/components/AudienceQuestionsForm.tsx"
      provides: "Audience questions section form"
    - path: "src/screens/operator/OperatorPanel.tsx"
      provides: "Episode file picker button for loading episodes during show"
      contains: "input.*file"
  key_links:
    - from: "src/screens/operator/OperatorPanel.tsx"
      to: "src/lib/episodeSchema.ts"
      via: "validates imported episode file"
      pattern: "validateEpisode\\|EpisodeSchema"
    - from: "src/screens/editor/EpisodeEditor.tsx"
      to: "src/screens/editor/components/DebateForm.tsx"
      via: "renders debate section form replacing placeholder"
      pattern: "DebateForm"
---

<objective>
Create the remaining section forms (Debate, Rapid Questions, Audience Questions), wire them into EpisodeEditor replacing the last placeholders, and add the episode file picker to the operator panel.

Purpose: Completes the episode editor with all section forms and adds the critical operator-side file picker for loading/swapping episodes during a live show.
Output: DebateForm, RapidQuestionsForm, AudienceQuestionsForm, operator panel file picker — editor fully functional
</objective>

<execution_context>
@/Users/jweaker/.config/opencode/get-shit-done/workflows/execute-plan.md
@/Users/jweaker/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-audio-episode-management/07-RESEARCH.md
@.planning/phases/07-audio-episode-management/07-05-SUMMARY.md
@src/screens/editor/EpisodeEditor.tsx
@src/screens/editor/components/QuestionListEditor.tsx
@src/lib/episodeSchema.ts
@src/screens/operator/OperatorPanel.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: DebateForm, RapidQuestionsForm, AudienceQuestionsForm and editor integration</name>
  <files>src/screens/editor/components/DebateForm.tsx, src/screens/editor/components/RapidQuestionsForm.tsx, src/screens/editor/components/AudienceQuestionsForm.tsx, src/screens/editor/EpisodeEditor.tsx</files>
  <action>
1. **Create `src/screens/editor/components/DebateForm.tsx`:**
   - Section card with title "النقاش" (Debate)
   - Single question — NOT a list. Shows:
     - Topic text (textarea for debate topic)
     - Duration input (defaults to episode settings debateDuration)
   - Props: `debate: Question`, `onChange: (d: Question) => void`, `errors`
   - Section id: `id="section-debate"`
   - Same card styling pattern as other section forms

2. **Create `src/screens/editor/components/RapidQuestionsForm.tsx`:**
   - Section card with title "الرشق السريع" (Rapid Questions)
   - Title input for the question set
   - Uses QuestionListEditor for the questions array with `showMarks={false}`
   - Note: This edits quickQuestions[0] (the first quick question set used by rapid questions)
   - Props: `quickQuestions: QuickQuestionSet[]`, `onChange: (q: QuickQuestionSet[]) => void`, `errors`
   - Section id: `id="section-rapid-questions"`

3. **Create `src/screens/editor/components/AudienceQuestionsForm.tsx`:**
   - Section card with title "أسئلة الجمهور" (Audience Questions)
   - Uses QuestionListEditor with `showMarks={false}`
   - Note at top: "هذه الأسئلة تُعرض بين الأقسام الرئيسية" (These questions are shown between main sections)
   - Props: `questions: Question[]`, `onChange: (q: Question[]) => void`, `errors`
   - Section id: `id="section-audience-questions"`

4. **Update `src/screens/editor/EpisodeEditor.tsx`:**
   - Replace the remaining placeholder divs (debate, rapid, audience) with real form components
   - Import DebateForm, RapidQuestionsForm, AudienceQuestionsForm
   - Wire each to the episode state via `updateParts()` helper
   - Verify NO placeholder divs remain — all sections are now real form components
  </action>
  <verify>
Run `npx tsc --noEmit` — zero type errors. Verify all 6 section forms are imported and rendered in EpisodeEditor.tsx. Verify no "Coming in next plan" placeholder text remains in EpisodeEditor.tsx. Run `npm run build` — succeeds.
  </verify>
  <done>All 6 section form components render. Debate is single-question. Rapid questions has title + list. Audience questions has note + list. No placeholders remain in editor.</done>
</task>

<task type="auto">
  <name>Task 2: Operator panel episode file picker for live show loading</name>
  <files>src/screens/operator/OperatorPanel.tsx</files>
  <action>
1. **Add episode file picker to `src/screens/operator/OperatorPanel.tsx`:**
   - Add a "تحميل حلقة" (Load Episode) button in the header bar (next to WindowLauncher)
   - Uses hidden `<input type="file" accept=".json" />` triggered by button click
   - On file select: FileReader → JSON.parse → validateEpisode → if valid, call `useShowStore.getState().setData(parsed)` to load into the live show
   - If invalid: show toast/alert with error summary (use window.alert for simplicity, or a small error banner)
   - Button icon: Lucide `FileUp` or `Upload`
   - This lets the operator load/swap episodes during a live show without going to /editor
   - Import `validateEpisode` from `@/lib/episodeSchema`
  </action>
  <verify>
Run `npx tsc --noEmit` — zero type errors. Run `npm run build` — succeeds. Verify OperatorPanel.tsx has file picker button and input[type=file accept=".json"]. Verify validateEpisode is imported and used for validation before loading.
  </verify>
  <done>Operator panel has file picker button. Loading validates JSON against episode schema before applying to show state. Invalid files show error. Full editor and operator-side loading complete.</done>
</task>

</tasks>

<verification>
1. `npx tsc --noEmit` passes
2. `npm run build` succeeds
3. /editor route shows ALL sections (no placeholders remain)
4. Debate form renders single question with duration
5. Rapid questions form renders title + question list
6. Audience questions form renders with explanatory note
7. Operator panel file picker validates and loads JSON episodes
8. Full end-to-end: create → edit → export → import in operator panel
</verification>

<success_criteria>
- DebateForm, RapidQuestionsForm, AudienceQuestionsForm all functional
- EpisodeEditor has no remaining placeholder sections
- Operator panel file picker validates JSON and loads into live show
- Complete editor workflow: blank/template/clone → edit all sections → export → load in operator
- TypeScript compiles, build succeeds
</success_criteria>

<output>
After completion, create `.planning/phases/07-audio-episode-management/07-06-SUMMARY.md`
</output>
