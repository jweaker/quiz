---
phase: 05-visual-system
plan: 05
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/animations/TypewriterText.tsx
  - src/components/animations/ScoreFlash.tsx
  - src/components/score/TeamScore.tsx
  - src/hooks/useScoreDelta.ts
  - src/components/audience/ScoreOverlay.tsx
  - src/screens/audience/AudienceDisplay.tsx
autonomous: true
requirements: [ANIM-01, ANIM-02, ANIM-03]
gap_closure: true

must_haves:
  truths:
    - "Arabic typewriter text renders with connected cursive letter forms"
    - "Score delta text (+N) appears below the score box, not colliding with team name"
    - "Team names are legibly sized on audience display"
    - "Rapid score changes each trigger their own animation in parallel"
    - "Negative score triggers screen shake with red flash overlay on audience display"
  artifacts:
    - path: "src/components/animations/TypewriterText.tsx"
      provides: "Arabic-safe typewriter reveal using clip-path or mask instead of character splitting"
      contains: "clip-path\\|mask\\|width"
    - path: "src/hooks/useScoreDelta.ts"
      provides: "Delta queue supporting parallel animations"
      contains: "queue\\|array\\|push\\|Map"
    - path: "src/screens/audience/AudienceDisplay.tsx"
      provides: "ScreenShake wrapping audience content"
      contains: "ScreenShake"
  key_links:
    - from: "src/hooks/useScoreDelta.ts"
      to: "src/components/score/TeamScore.tsx"
      via: "delta array/queue consumed by multiple ScoreFlash instances"
      pattern: "map.*ScoreFlash\\|deltas"
    - from: "src/screens/audience/AudienceDisplay.tsx"
      to: "src/components/animations/ScreenShake.tsx"
      via: "ScreenShake wrapper triggered on negative score"
      pattern: "ScreenShake"
---

<objective>
Fix three audience display visual issues identified in UAT: Arabic typewriter text renders disconnected letters, score celebration animations have positioning/sizing/stacking problems, and negative scores lack the red flash overlay.

Purpose: Close UAT gaps 1, 3, and 4 (tests 1, 3, 4) so the audience display is broadcast-ready.
Output: Fixed TypewriterText, ScoreFlash positioning, parallel score animations, and ScreenShake wiring.
</objective>

<execution_context>
@/Users/jweaker/.claude/get-shit-done/workflows/execute-plan.md
@/Users/jweaker/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-visual-system/05-01-SUMMARY.md
@.planning/phases/05-visual-system/05-02-SUMMARY.md
@.planning/debug/arabic-typewriter-disconnected.md
@.planning/debug/score-celebration-issues.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix Arabic TypewriterText with clip-path reveal</name>
  <files>src/components/animations/TypewriterText.tsx</files>
  <action>
  Rewrite TypewriterText to preserve Arabic contextual shaping. The current implementation splits text into individual characters via `text.split('')` and wraps each in a separate `motion.span` — this breaks Arabic ligatures because the browser cannot apply contextual forms across DOM boundaries.

  Replace the character-splitting approach with a clip-path or width-based reveal animation that keeps all text in a single contiguous DOM element:

  1. Render the full text in a single `span` element (preserving RTL direction)
  2. Use a `motion.div` wrapper with animated `clipPath` (e.g., `inset(0 X% 0 0)` for RTL, revealing from right to left) or animated `width` from 0 to 100% with `overflow: hidden`
  3. Calculate total animation duration from text length and stagger speed (e.g., `text.length * speed`)
  4. Keep the existing props interface (text, speed, className, onComplete)
  5. Keep the `dir="rtl"` attribute on the container
  6. For the clip-path approach: animate from `inset(0 100% 0 0)` to `inset(0 0% 0 0)` for RTL reveal (clips from left side, revealing right-to-left)
  7. Respect `usePrefersReducedMotion` — if reduced motion, show full text immediately
  8. Keep the slide-up entrance animation on the container (y: 20 to 0) — only the character reveal changes

  Test with Arabic text "بشائر المعرفة" — letters must render connected (contextual forms), not isolated.
  </action>
  <verify>Run `pnpm build` to confirm no type errors. Visually: Arabic text should show connected cursive forms during the reveal animation.</verify>
  <done>TypewriterText renders Arabic text with connected letter forms using clip-path/width reveal instead of character splitting. Existing props interface preserved.</done>
</task>

<task type="auto">
  <name>Task 2: Fix score celebrations — positioning, sizing, parallel animations, and ScreenShake</name>
  <files>
    src/components/animations/ScoreFlash.tsx
    src/components/score/TeamScore.tsx
    src/hooks/useScoreDelta.ts
    src/components/audience/ScoreOverlay.tsx
    src/screens/audience/AudienceDisplay.tsx
  </files>
  <action>
  Fix four score celebration issues from UAT:

  **A. Move delta text below score box (ScoreFlash.tsx):**
  Change positioning from `absolute -top-4 left-1/2 -translate-x-1/2 -translate-y-full` to position BELOW the score box. Use `absolute -bottom-4 left-1/2 -translate-x-1/2 translate-y-full` (or similar — delta text should float below the score number, away from the team name above). Update the motion animation direction: initial y should come from below (positive y) rather than above. Apply this change to both the positive delta (confetti) path and the negative delta (red text) path, and the reduced-motion fallback.

  **B. Increase team name font size (TeamScore.tsx):**
  On line ~36, change `fontSize: 'clamp(0.8rem, 2vw, 2.5rem)'` to `fontSize: 'clamp(1.2rem, 3vw, 3rem)'` — increase minimum from 0.8rem to 1.2rem and scale factor from 2vw to 3vw for better audience legibility.

  **C. Parallel score animations (useScoreDelta.ts + TeamScore.tsx):**
  Rewrite useScoreDelta to support multiple simultaneous deltas instead of replacing:
  - Change from single `delta` state to an array/Map of deltas, each with a unique key (e.g., `Date.now()` or incrementing counter) and value
  - On each score change, PUSH a new delta entry instead of replacing
  - Each delta auto-removes itself after the animation duration (2 seconds)
  - Do NOT clear previous timeouts — each delta manages its own lifecycle
  - Return the deltas array (not a single value)
  - In TeamScore.tsx, map over the deltas array to render multiple ScoreFlash instances simultaneously, each with a unique React key
  - ScoreFlash should accept a key prop and render independently

  **D. Wire ScreenShake for negative scores (ScoreOverlay.tsx + AudienceDisplay.tsx):**
  - In AudienceDisplay.tsx (or ScoreOverlay.tsx), wrap the score display area in the existing `ScreenShake` component
  - Add state tracking for negative score trigger: watch for score decreases and increment a trigger counter
  - Pass the trigger counter to ScreenShake's `trigger` prop
  - Import ScreenShake from `@/components/animations`
  - The ScreenShake component already handles the red flash overlay internally — just needs to be wired
  </action>
  <verify>Run `pnpm build` to confirm no type errors. Verify: delta text appears below score box, team names are larger, rapid score additions show overlapping animations, negative scores trigger red flash overlay.</verify>
  <done>Score delta positioned below score box. Team names sized at minimum 1.2rem. Rapid score changes produce parallel animations. Negative scores trigger ScreenShake with red flash overlay.</done>
</task>

</tasks>

<verification>
- `pnpm build` succeeds with zero errors
- Arabic typewriter text shows connected cursive forms (not isolated letters)
- Score delta (+N) text appears below the score box, not overlapping team names
- Team names are visibly larger on audience display
- Adding points 3 times rapidly shows 3 simultaneous flying delta animations
- Deducting points triggers horizontal screen shake with red flash overlay
</verification>

<success_criteria>
UAT tests 1, 3, and 4 would pass on re-test. Arabic text renders correctly, score celebrations are properly positioned with parallel animation support, and negative scores produce the red flash effect.
</success_criteria>

<output>
After completion, create `.planning/phases/05-visual-system/05-05-SUMMARY.md`
</output>
