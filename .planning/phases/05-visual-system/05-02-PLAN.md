---
phase: 05-visual-system
plan: 02
type: execute
wave: 2
depends_on: ["05-01"]
files_modified:
  - src/components/animations/ScoreFlash.tsx
  - src/components/animations/ScreenShake.tsx
  - src/components/animations/MinefieldLayout.tsx
  - src/components/score/TeamScore.tsx
  - src/components/audience/ScoreOverlay.tsx
  - src/screens/audience/AudienceDisplay.tsx
autonomous: true

must_haves:
  truths:
    - "Score changes trigger punchy celebration effects (particles/flash/scale pop) on audience display"
    - "Minefield section has dark background with spotlight effect and subtle pulsing glow"
    - "Wrong answer in Minefield triggers screen shake plus red flash overlay"
    - "Score stakes (+16/-8/0) flash dramatically on answer, not permanently visible"
    - "Celebrations use universal gold/white palette (no team-specific colors)"
    - "All effects run at 60fps without frame drops on MacBook Pro"
  artifacts:
    - path: "src/components/animations/ScoreFlash.tsx"
      provides: "Score celebration with confetti particles and flash overlay"
      exports: ["ScoreFlash"]
    - path: "src/components/animations/ScreenShake.tsx"
      provides: "Screen shake effect wrapper with red flash for wrong answers"
      exports: ["ScreenShake"]
    - path: "src/components/animations/MinefieldLayout.tsx"
      provides: "Minefield visual treatment with dark accents and pulsing glow"
      exports: ["MinefieldLayout"]
  key_links:
    - from: "src/components/score/TeamScore.tsx"
      to: "src/components/animations/ScoreFlash.tsx"
      via: "Renders ScoreFlash when score changes with large delta"
      pattern: "ScoreFlash"
    - from: "src/components/animations/ScreenShake.tsx"
      to: "motion/react useAnimationControls"
      via: "Imperative shake animation on trigger prop change"
      pattern: "useAnimationControls"
    - from: "src/components/animations/MinefieldLayout.tsx"
      to: "src/lib/animationPresets.ts"
      via: "Uses energetic easing and animation presets"
      pattern: "animationPresets"
---

<objective>
Add score celebration effects and Minefield visual treatment to the audience display. Score changes trigger punchy confetti/flash/scale-pop celebrations. Minefield section gets dark, suspenseful accents with screen shake and red flash on wrong answers.

Purpose: These are the core "broadcast-quality" effects that make the show feel like real TV. Score celebrations create energy, Minefield effects create tension. Both are key user decisions: "energetic & bold tone" and "wrong answer: screen shake + red flash."

Output: ScoreFlash celebration component, ScreenShake effect, MinefieldLayout with pulsing glow, enhanced TeamScore with celebration integration, stakes flash overlay.
</objective>

<execution_context>
@/Users/jweaker/.claude/get-shit-done/workflows/execute-plan.md
@/Users/jweaker/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-visual-system/05-CONTEXT.md
@.planning/phases/05-visual-system/05-RESEARCH.md
@.planning/phases/05-visual-system/05-01-SUMMARY.md
@src/components/score/TeamScore.tsx
@src/components/audience/ScoreOverlay.tsx
@src/lib/animationPresets.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: ScoreFlash celebration with confetti and flash overlay</name>
  <files>
    src/components/animations/ScoreFlash.tsx
    src/components/score/TeamScore.tsx
    src/components/audience/ScoreOverlay.tsx
  </files>
  <action>
    Create `src/components/animations/ScoreFlash.tsx`:
    - Props: `delta: number` (score change amount), `onComplete?: () => void`
    - Import `ConfettiBoom` from `react-confetti-boom` and `motion` from `motion/react`
    - Import `usePrefersReducedMotion` from `@/hooks/usePrefersReducedMotion`
    - When `delta > 0` (positive score):
      - Render ConfettiBoom with `particleCount={40}`, `colors={['#FFD700', '#FFA500', '#FFFFFF', '#FFE4B5']}` (gold/orange/white -- universal, no team colors per user decision)
      - Render a `motion.div` showing `+{delta}` text with scale animation: `[1, 1.8, 1.2]` over 500ms with emphasized easing, then fade out
      - Text uses large bold font, gold color (`text-amber-400`)
    - When `delta < 0` (negative/wrong answer):
      - No confetti
      - Render `motion.div` showing `{delta}` in red (`text-red-500`) with shake + fade
    - When `prefersReducedMotion` is true, show the delta text without animation (instant appear + disappear after 1s)
    - Auto-cleanup: component unmounts itself after animation completes via onComplete callback

    Update `src/components/score/TeamScore.tsx` (audience variant only):
    - Import ScoreFlash
    - When `delta !== null` AND variant is `'audience'`, render `<ScoreFlash delta={delta} />` positioned absolutely above the score box (replacing the simple delta text div)
    - Keep the existing CSS animation (`animate-delta-fade`) for the operator variant (operator doesn't need confetti)
    - The existing `animate-score-pop` CSS class on score number stays -- Motion celebrations layer on top

    Update `src/components/audience/ScoreOverlay.tsx`:
    - Import `motion` from `'motion/react'` and `AnimatePresence`
    - Wrap the score container in `motion.div` with a subtle entrance animation (slide down + fade in) on mount using `animationPresets.entrance`
    - This ensures scores animate in when the audience display loads
  </action>
  <verify>
    `pnpm build` compiles without errors. On audience display, manually trigger a score change (via operator panel scoring buttons or keyboard 1-8 presets). Positive score change should show gold confetti burst + floating score delta. Negative score change should show red delta text with shake. No confetti on negative. Operator panel score display should remain unchanged (no confetti, just existing delta indicator).
  </verify>
  <done>Score celebrations show confetti + flash on audience display for positive scores. Red shake effect for negative scores. Universal gold/white palette used (no team colors). Operator variant unchanged. Audience ScoreOverlay has entrance animation.</done>
</task>

<task type="auto">
  <name>Task 2: Minefield visual treatment with screen shake and stakes flash</name>
  <files>
    src/components/animations/ScreenShake.tsx
    src/components/animations/MinefieldLayout.tsx
    src/screens/audience/AudienceDisplay.tsx
  </files>
  <action>
    Create `src/components/animations/ScreenShake.tsx`:
    - Props: `trigger: number` (incrementing counter to re-trigger), `intensity?: 'light' | 'heavy'` (default 'heavy'), `children: React.ReactNode`
    - Import `motion, useAnimationControls` from `'motion/react'` and `usePrefersReducedMotion`
    - Uses `useAnimationControls()` for imperative shake
    - `useEffect` watching `trigger`: when trigger changes (and > 0), fire shake animation:
      - Heavy: `x: [0, -12, 12, -12, 12, -6, 6, 0]` over 400ms with easeInOut
      - Light: `x: [0, -4, 4, -4, 0]` over 250ms
    - Simultaneously render a red flash overlay `motion.div` with `position: absolute, inset: 0, pointerEvents: none`:
      - `initial={{ opacity: 0 }}`, animate `opacity: [0, 0.6, 0]` with timing `[0, 0.3, 1]` over 500ms
      - Red overlay `backgroundColor: 'rgba(220, 38, 38, 0.5)'`
    - If `prefersReducedMotion`, skip shake transform but still show red flash (brief red border flash instead of overlay)
    - Children rendered inside the `motion.div` that receives shake

    Create `src/components/animations/MinefieldLayout.tsx`:
    - Props: `active: boolean`, `children: React.ReactNode`
    - When `active` is true:
      - Dark background overlay with `bg-black/90`
      - Subtle pulsing glow animation on the outer container: `boxShadow` animates between `0 0 20px rgba(220, 38, 38, 0.2)` and `0 0 50px rgba(220, 38, 38, 0.5)` with `repeat: Infinity, duration: 2, ease: 'easeInOut'`
      - Spotlight effect: radial gradient overlay from transparent center to dark edges, giving a "spotlight on question" feel
      - Per user decision: "same layout structure as other sections, but with tension/suspense accents"
    - When `active` is false, render children without effects
    - Export a `StakesFlash` sub-component:
      - Props: `value: number | null` (the +16, -8, or 0 to display), `type: 'correct' | 'wrong' | 'partial'`
      - When `value` is not null, renders a large centered overlay text:
        - Correct (+16): green text, scale pop animation `[0.5, 1.3, 1]`, then fades out after 1.5s
        - Wrong (-8): red text with `ScreenShake`-like emphasis, stays 2s for dramatic impact
        - Partial (0): yellow/amber text, simple fade in/out
      - Text uses very large font (6-8rem), bold, with drop shadow for broadcast visibility
      - Per user decision: "score stakes flash on answer -- shown dramatically when answer is given, not permanently visible"
      - Auto-hides after display duration

    Update `src/screens/audience/AudienceDisplay.tsx`:
    - Import MinefieldLayout, ScreenShake
    - Add these as available wrappers around the content area. They are NOT wired to real section state yet (that happens in Plan 05-04 when section state is added to the store). For now:
      - Add a comment: `{/* MinefieldLayout and ScreenShake wrap content when currentSection === 'minefield' -- wired in Plan 05-04 */}`
      - Export the components from the animations barrel so they are importable
    - Create `src/components/animations/index.ts` barrel file exporting: TypewriterText, WipeTransition, ScoreFlash, ScreenShake, MinefieldLayout, StakesFlash
  </action>
  <verify>
    `pnpm build` compiles without errors. Verify all animation components export from the barrel `src/components/animations/index.ts`. Test ScreenShake by temporarily rendering it on AudienceDisplay with a hardcoded trigger -- the screen should shake and flash red. Test MinefieldLayout by wrapping a div -- should show dark background with pulsing red glow. StakesFlash with value={16} should show large green "+16" text that fades after 1.5s.
  </verify>
  <done>ScreenShake triggers shake + red flash on audience display. MinefieldLayout provides dark/suspense accents with pulsing glow. StakesFlash shows dramatic +16/-8/0 overlays that auto-hide. All effects respect prefers-reduced-motion. Animation components barrel-exported.</done>
</task>

</tasks>

<verification>
1. `pnpm build` succeeds with zero TypeScript errors
2. Audience display score celebrations fire on score change (confetti + delta flash)
3. ScreenShake produces visible shake + red flash overlay
4. MinefieldLayout shows dark suspense theme with pulsing glow
5. StakesFlash shows dramatic point values that auto-hide
6. All effects use universal gold/white/red palette (no team-specific colors)
7. With prefers-reduced-motion enabled, effects are either instant or use non-motion alternatives
</verification>

<success_criteria>
- Score celebrations: punchy confetti + flash on positive scores, red shake on negative (per user decision)
- Minefield: accent changes only (dark bg, spotlight, pulsing glow) -- same layout structure
- Wrong answer: screen shake + red flash (per user decision)
- Stakes flash: dramatic +16/-8/0 that appear on answer and auto-hide (per user decision)
- Universal palette: gold/orange/white for celebrations, no team colors (per user decision)
- 60fps performance maintained
</success_criteria>

<output>
After completion, create `.planning/phases/05-visual-system/05-02-SUMMARY.md`
</output>
