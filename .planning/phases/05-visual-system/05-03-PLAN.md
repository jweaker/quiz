---
phase: 05-visual-system
plan: 03
type: execute
wave: 2
depends_on: ["05-01"]
files_modified:
  - src/screens/operator/OperatorControls.tsx
  - src/components/operator/KeyboardShortcutOverlay.tsx
  - src/lib/shortcutRegistry.ts
  - src/components/operator/ScoringPanel.tsx
  - src/components/operator/TimerPanel.tsx
  - src/components/operator/ConfidenceMonitor.tsx
  - src/screens/operator/OperatorPanel.tsx
autonomous: true

must_haves:
  truths:
    - "Operator panel fits on one screen without scrolling for core operations"
    - "Scores, timer status, and confidence monitor are always visible regardless of active section"
    - "Control area adapts to show only relevant controls for the current section context"
    - "Primary controls show their keyboard shortcut inline (e.g., [T] Timer)"
    - "Pressing ? toggles a full keyboard shortcut reference overlay"
    - "Layout feels like a broadcast switcher: small text, compact controls, information-dense"
  artifacts:
    - path: "src/screens/operator/OperatorControls.tsx"
      provides: "Redesigned context-adaptive operator layout with persistent + adaptive zones"
      min_lines: 80
    - path: "src/components/operator/KeyboardShortcutOverlay.tsx"
      provides: "Full keyboard shortcut reference overlay toggled by ? key"
      exports: ["KeyboardShortcutOverlay"]
    - path: "src/lib/shortcutRegistry.ts"
      provides: "Centralized shortcut definitions used by both inline hints and overlay"
      exports: ["SHORTCUTS", "ShortcutCategory"]
  key_links:
    - from: "src/screens/operator/OperatorControls.tsx"
      to: "src/lib/shortcutRegistry.ts"
      via: "Imports shortcut definitions for inline hints"
      pattern: "import.*shortcutRegistry"
    - from: "src/components/operator/KeyboardShortcutOverlay.tsx"
      to: "src/lib/shortcutRegistry.ts"
      via: "Imports all shortcuts for full reference display"
      pattern: "import.*SHORTCUTS"
    - from: "src/screens/operator/OperatorControls.tsx"
      to: "src/components/operator/ScoringPanel.tsx"
      via: "Renders ScoringPanel in adaptive zone"
      pattern: "ScoringPanel"
---

<objective>
Redesign the operator panel from its current broken vertical-scroll layout into a compact, mission-control-density interface. Create a persistent zone (scores + timer + confidence monitor) that is always visible, and a context-adaptive zone that shows only relevant controls for the current section. Add keyboard shortcut inline hints on all controls and a ? overlay for full reference.

Purpose: CRITICAL user feedback: "operator panel layout is badly designed -- long vertical scroll, redundant options, poor use of space." This plan fixes it by redesigning for a broadcast switcher: compact, no-scroll, information-dense. The operator must glance and act quickly during live TV.

Output: Completely redesigned OperatorControls with persistent + adaptive zones, keyboard shortcut inline hints on all buttons, ? overlay with full reference, compact ScoringPanel and TimerPanel.
</objective>

<execution_context>
@/Users/jweaker/.claude/get-shit-done/workflows/execute-plan.md
@/Users/jweaker/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-visual-system/05-CONTEXT.md
@.planning/phases/05-visual-system/05-RESEARCH.md
@.planning/phases/05-visual-system/05-01-SUMMARY.md
@src/screens/operator/OperatorControls.tsx
@src/screens/operator/OperatorPanel.tsx
@src/components/operator/ScoringPanel.tsx
@src/components/operator/TimerPanel.tsx
@src/components/operator/ConfidenceMonitor.tsx
@src/hooks/useScoreControls.ts
@src/state/showStore.ts
@src/state/timerStore.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create shortcut registry and redesign OperatorControls with context-adaptive layout</name>
  <files>
    src/lib/shortcutRegistry.ts
    src/screens/operator/OperatorControls.tsx
    src/components/operator/ScoringPanel.tsx
    src/components/operator/TimerPanel.tsx
  </files>
  <action>
    Create `src/lib/shortcutRegistry.ts`:
    - Define `ShortcutCategory` type: 'scoring' | 'timer' | 'navigation' | 'chess-clock' | 'general'
    - Define `ShortcutDef` interface: `{ keys: string, label: string, category: ShortcutCategory, description?: string }`
    - Export `SHORTCUTS` as a `Record<string, ShortcutDef>` containing ALL existing shortcuts from the app:
      - Scoring: 1 (+1), 2 (+2), 5 (+5), 8 (+8), 0 (+10), Shift+5 (+15), Shift+6 (+16), - (-8), Space (toggle turn), Cmd+Shift+S (swap sides), Cmd+Z (undo), Cmd+Shift+Z (redo)
      - Timer: T (start/pause countdown), Shift+T (reset countdown)
      - Chess Clock: [ (start right), ] (start left), \ (switch clock), P (pause), Shift+P (reset)
      - Pass: G (pass verse), V (accept pass), X (reject pass)
      - General: Cmd+Shift+A (open audience), ? (shortcuts overlay), Escape (back)
    - Export `getShortcutsByCategory(category: ShortcutCategory): ShortcutDef[]`
    - Export `formatShortcutKey(keys: string): string` helper that converts key names to display format (e.g., 'mod+shift+s' -> macOS: '⌘⇧S')

    Redesign `src/screens/operator/OperatorControls.tsx` completely:
    - Remove the current vertical stack layout (header -> scoreboard -> ScoringPanel -> TimerPanel)
    - New layout structure (NO SCROLLING for core operations):

    ```
    ┌─────────────────────────────────────────┐
    │  PERSISTENT ZONE (always visible)        │
    │  ┌──────────┬──────────┬───────────────┐ │
    │  │ Team R   │ Timer    │ Quick Actions  │ │
    │  │ Score    │ Status   │ [T]Timer       │ │
    │  │ Team L   │ display  │ [Space]Turn    │ │
    │  │ Score    │          │ [?]Shortcuts   │ │
    │  └──────────┴──────────┴───────────────┘ │
    ├─────────────────────────────────────────┤
    │  ADAPTIVE ZONE (changes per context)     │
    │  [Current section-specific controls]     │
    │  e.g., scoring presets, chess clock,     │
    │  pass mechanics, etc.                    │
    └─────────────────────────────────────────┘
    ```

    - **Persistent zone** (top ~40% of height): `flex` row layout
      - Left column: Compact team scores -- show both teams side-by-side with score numbers, team names in small text, active team highlighted with primary border. Use TeamScore variant="operator" but made more compact (remove the large padding, use text-3xl instead of text-5xl)
      - Center column: Timer status summary -- show current countdown remaining (or chess clock times) as a compact readout. Not the full TimerPanel, just the numbers and a running/paused indicator. Small text, tabular-nums, western-numerals
      - Right column: Quick action buttons in a tight vertical stack with inline shortcut hints. Each button: small, with `<kbd>` showing shortcut inline. Include: toggle turn [Space], undo [⌘Z], redo [⌘⇧Z], swap sides [⌘⇧S], settings gear icon

    - **Adaptive zone** (bottom ~60%): Renders content based on current mode/section
      - Default: ScoringPanel (scoring presets + custom input)
      - When timer mode active: TimerPanel countdown controls
      - When chess clock mode active: Chess clock controls + pass mechanics
      - Use `AnimatePresence` from motion/react for smooth transitions between adaptive zone content (200ms fade)
      - Keep existing `useScoreControls()` hook registration in OperatorControls

    Compact `src/components/operator/ScoringPanel.tsx`:
    - Remove the section header and descriptive text (operator knows what these are)
    - Make preset buttons smaller: `size="xs"` or custom compact size (h-7 px-2 text-xs)
    - Put all scoring presets in a single tight row (flex-wrap gap-1 instead of gap-2)
    - Combine positive and negative presets into one row
    - Move scoring history to a collapsible section (collapsed by default -- saves space)
    - Inline shortcut hints on each button using `<kbd>` tags as they already have

    Compact `src/components/operator/TimerPanel.tsx`:
    - Remove the large centered countdown display (it's in the persistent zone already)
    - Timer controls become a horizontal row of compact buttons
    - Duration presets in a compact button group
    - Chess clock: Two-column compact display (smaller text, tighter padding)
    - Remove mode toggle from TimerPanel (mode selection moves to the adaptive zone switcher in OperatorControls)
    - Custom duration input stays but inline (not its own section)
  </action>
  <verify>
    `pnpm build` compiles without errors. Open `http://localhost:5173/operator` -- the operator panel should show:
    1. Persistent zone at top: scores + timer status + quick actions visible without scrolling
    2. Adaptive zone below: scoring controls by default
    3. All buttons have inline keyboard shortcut hints in `<kbd>` tags
    4. No vertical scrolling needed for core operations (scores, timer, scoring buttons all visible on a 13" MacBook Pro screen)
    5. All existing keyboard shortcuts still work (1-8, Space, T, etc.)
  </verify>
  <done>Operator panel redesigned with persistent zone (scores + timer + quick actions) always visible. Adaptive zone shows context-relevant controls. No scrolling for core operations. Small text, compact controls, mission-control density. All existing keyboard shortcuts preserved.</done>
</task>

<task type="auto">
  <name>Task 2: Keyboard shortcut overlay and confidence monitor integration</name>
  <files>
    src/components/operator/KeyboardShortcutOverlay.tsx
    src/screens/operator/OperatorPanel.tsx
    src/components/operator/ConfidenceMonitor.tsx
  </files>
  <action>
    Create `src/components/operator/KeyboardShortcutOverlay.tsx`:
    - Import `useHotkeys` from `react-hotkeys-hook`, `motion, AnimatePresence` from `motion/react`
    - Import `SHORTCUTS, getShortcutsByCategory, formatShortcutKey` from `@/lib/shortcutRegistry`
    - Local state `isOpen` toggled by `shift+/` (which is the `?` key)
    - Use `useHotkeys('shift+/', () => setIsOpen(prev => !prev))` with `enableOnFormTags: false`
    - When open, render:
      - Full-screen backdrop: `fixed inset-0 z-50`, `backdrop-blur-sm bg-black/60`
      - Click backdrop to close
      - Centered modal: `motion.div` with `initial={{ scale: 0.9, opacity: 0 }} animate={{ scale: 1, opacity: 1 }}` using energetic easing
      - Inside modal: multi-column grid of shortcuts grouped by category
        - Each category: bold header (Arabic label), then list of shortcut items
        - Each item: `<kbd>` with key display on left, description on right
        - Categories: التحكم بالنقاط (Scoring), المؤقتات (Timer), ساعة الشطرنج (Chess Clock), تمرير الأبيات (Pass), عام (General)
      - Small text throughout for density. Max-width 600px
      - Close button (X) top-right corner, plus Escape to close
      - Footer text: "اضغط ? لإغلاق" (Press ? to close)

    Update `src/screens/operator/OperatorPanel.tsx`:
    - Import and render `<KeyboardShortcutOverlay />` at top level (inside the main div, after DisconnectBanner)
    - This ensures the overlay is available on all operator screens

    Update `src/components/operator/ConfidenceMonitor.tsx`:
    - Ensure the confidence monitor (audience preview) remains functional within the resizable right panel
    - The confidence monitor is already in the persistent zone by virtue of the ResizablePanelGroup in OperatorPanel.tsx -- it stays as-is
    - Add a subtle border treatment and "PREVIEW" label in small text at top of confidence monitor panel for clarity
    - The confidence monitor size persists via operatorStore (already implemented)
  </action>
  <verify>
    `pnpm build` compiles without errors. On operator panel:
    1. Press `?` (Shift+/) -- full shortcut overlay appears with all shortcuts grouped by category
    2. Press `?` again or click backdrop -- overlay closes
    3. Press Escape while overlay open -- closes
    4. All shortcut keys display correctly with proper symbols (⌘, ⇧, etc.)
    5. Confidence monitor panel visible on right side with "PREVIEW" label
    6. Overlay does not interfere with other keyboard shortcuts when closed
  </verify>
  <done>Keyboard shortcut overlay shows full reference grouped by category when ? pressed. Overlay uses Motion animations. Confidence monitor labeled clearly in resizable panel. All keyboard shortcuts surfaced for operator discovery.</done>
</task>

</tasks>

<verification>
1. `pnpm build` succeeds with zero TypeScript errors
2. Operator panel: no scrolling needed for core operations on 13" MacBook Pro
3. Persistent zone: scores + timer status + quick actions always visible
4. Adaptive zone: shows scoring controls by default, can show timer/chess clock
5. All buttons have inline `<kbd>` shortcut hints
6. ? overlay shows all shortcuts grouped by category
7. All existing keyboard shortcuts still function correctly
8. Confidence monitor in right panel with clear label
</verification>

<success_criteria>
- Operator panel redesigned per user decision: context-adaptive layout, mission-control density
- Scores + timer + confidence monitor always visible (per user decision)
- No scrolling for core operations (per user decision: "everything on one screen")
- Keyboard shortcuts: inline hints + ? overlay (per user decision)
- All existing functionality preserved (scoring, timer, chess clock, pass mechanics)
- Compact, information-dense layout like a broadcast switcher
</success_criteria>

<output>
After completion, create `.planning/phases/05-visual-system/05-03-SUMMARY.md`
</output>
