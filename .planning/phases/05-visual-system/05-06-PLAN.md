---
phase: 05-visual-system
plan: 06
type: execute
wave: 1
depends_on: []
files_modified:
  - src/screens/operator/OperatorControls.tsx
  - src/lib/shortcutRegistry.ts
  - src/components/operator/KeyboardShortcutOverlay.tsx
autonomous: true
requirements: [CTRL-02, CTRL-03, ANIM-05]
gap_closure: true

must_haves:
  truths:
    - "Operator score values are directly editable text inputs filling the container"
    - "A keyboard shortcut cycles through adaptive zone tabs"
    - "Tab labels are compact text-only without icons"
    - "Pressing ? (shift+/) opens the keyboard shortcut overlay"
    - "Cmd+Right goes backward (previous) and Cmd+Left goes forward (next) for RTL navigation"
  artifacts:
    - path: "src/screens/operator/OperatorControls.tsx"
      provides: "Editable score inputs, tab shortcut, RTL-swapped nav, icon-free tabs"
      contains: "input.*number\\|type=.number"
    - path: "src/components/operator/KeyboardShortcutOverlay.tsx"
      provides: "Shortcut overlay triggered by shift+/"
      contains: "shift\\+/"
    - path: "src/lib/shortcutRegistry.ts"
      provides: "Updated registry with tab-cycle and corrected ? key"
      contains: "tab-cycle\\|shift\\+/"
  key_links:
    - from: "src/screens/operator/OperatorControls.tsx"
      to: "src/state/showStore.ts"
      via: "input onChange wired to setRightScore/setLeftScore"
      pattern: "setRightScore\\|setLeftScore"
    - from: "src/lib/shortcutRegistry.ts"
      to: "src/components/operator/KeyboardShortcutOverlay.tsx"
      via: "registry key definition consumed by overlay hotkey binding"
      pattern: "shift\\+/"
---

<objective>
Fix four operator panel issues from UAT: make scores directly editable, add tab cycling shortcut, fix ? key for shortcut overlay, and swap Cmd+Right/Left for RTL-correct section navigation.

Purpose: Close UAT gaps 5, 6, 7, and 9 (tests 6, 7, 8, 11) so the operator panel is fully functional.
Output: Editable score inputs, working tab and shortcut overlay hotkeys, RTL-correct navigation.
</objective>

<execution_context>
@/Users/jweaker/.claude/get-shit-done/workflows/execute-plan.md
@/Users/jweaker/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-visual-system/05-03-SUMMARY.md
@.planning/phases/05-visual-system/05-04-SUMMARY.md
@.planning/debug/operator-panel-issues.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Editable score inputs and score container layout</name>
  <files>src/screens/operator/OperatorControls.tsx</files>
  <action>
  Fix UAT test 6 — operator scores must be directly editable and container must fill space.

  **A. Replace score `<p>` with `<input>` (lines ~120 and ~138):**
  - Replace `<p className="text-3xl font-bold tabular-nums leading-tight">{rightScore}</p>` with an `<input>` element
  - Use `type="number"` so numeric keyboard appears
  - Wire `value` to the score state and `onChange` to `setRightScore(Number(e.target.value))` / `setLeftScore(Number(e.target.value))`
  - Style the input to match existing appearance: `text-3xl font-bold tabular-nums leading-tight text-center bg-transparent border-none outline-none w-full`
  - Add `[appearance:textfield]` class to remove number spinners, plus `[&::-webkit-outer-spin-button]:appearance-none [&::-webkit-inner-spin-button]:appearance-none`
  - Do the same for both right and left score displays

  **B. Vertically center score containers (lines ~110-114 and ~128-132):**
  - Add `flex flex-col justify-center items-center` to the score container div (the one with `px-3 py-2 text-center`)
  - Ensure the container fills its allocated space with `h-full` if not already present
  - This makes the score number and label vertically centered in the box instead of stuck at the top
  </action>
  <verify>Run `pnpm build` to confirm no type errors. Score containers should have `<input type="number">` elements. Containers should have vertical centering classes.</verify>
  <done>Score values are editable via number inputs. Score containers are vertically centered and fill available space.</done>
</task>

<task type="auto">
  <name>Task 2: Fix shortcuts — tab cycling, ? overlay, RTL navigation, icon removal</name>
  <files>
    src/screens/operator/OperatorControls.tsx
    src/lib/shortcutRegistry.ts
    src/components/operator/KeyboardShortcutOverlay.tsx
  </files>
  <action>
  Fix UAT tests 7, 8, and 11 — four shortcut-related issues.

  **A. Add tab cycling shortcut (OperatorControls.tsx + shortcutRegistry.ts):**
  - In shortcutRegistry.ts, add a new shortcut entry for tab cycling. Use the key `Tab` (the Tab key) or `Backquote` (backtick) — NOT backslash since that is already used for chess clock switching. Suggested: use backtick (`) as it is near Tab and unused. Register as: `'tab-cycle': { keys: 'Backquote', label: 'تبديل اللوحة', category: 'navigation' }`
  - In OperatorControls.tsx, add `useHotkeys('Backquote', ...)` that cycles `adaptiveMode` through `'scoring' -> 'countdown' -> 'chess-clock' -> 'scoring'`
  - Use `enableOnFormTags: false` to prevent firing while typing in score inputs

  **B. Fix ? key for shortcut overlay (KeyboardShortcutOverlay.tsx + shortcutRegistry.ts):**
  - In KeyboardShortcutOverlay.tsx, find the useHotkeys call that listens for `?` and change it to `shift+/` — react-hotkeys-hook requires the modifier+key form, not the resulting character
  - In shortcutRegistry.ts, update the shortcut-overlay entry keys from `'?'` to `'shift+/'` (keep the display label as `?` for the overlay UI)

  **C. Swap Cmd+Right/Left for RTL navigation (OperatorControls.tsx):**
  - Find the useHotkeys bindings for section navigation (Cmd+Right and Cmd+Left)
  - SWAP them: `meta+right` should call `prevSection()` (right arrow = go backward in RTL) and `meta+left` should call `nextSection()` (left arrow = go forward in RTL)
  - Also update shortcutRegistry.ts descriptions if they reference direction, to avoid confusion
  - The user's mental model: Right arrow key = move rightward on screen = earlier section (RTL). Left arrow key = move leftward = later section (RTL).

  **D. Remove icons from adaptive zone tab buttons (OperatorControls.tsx):**
  - Find the tab buttons for countdown and chess-clock modes (lines ~267, ~276)
  - Remove the `<Timer className="size-3 me-1" />` and `<Users className="size-3 me-1" />` icon components
  - Keep only the text labels: "النقاط", "عد تنازلي", "المطاردة"
  - Remove unused icon imports (Timer, Users from lucide-react) if they are no longer used elsewhere in the file
  </action>
  <verify>Run `pnpm build` to confirm no type errors and no unused imports. Verify: backtick key cycles tabs, shift+/ opens shortcut overlay, Cmd+Right goes backward, tab labels are text-only.</verify>
  <done>Tab cycling works via backtick key. ? (shift+/) opens shortcut overlay. Cmd+Right/Left swapped for RTL. Tab buttons are text-only without icons.</done>
</task>

</tasks>

<verification>
- `pnpm build` succeeds with zero errors
- Score values in operator panel are editable number inputs
- Score containers fill vertical space with centered content
- Pressing backtick (`) cycles between scoring/countdown/chess-clock tabs
- Pressing ? (shift+/) opens the keyboard shortcut overlay
- Pressing Escape closes the shortcut overlay
- Cmd+Right navigates to the previous section (backward in RTL)
- Cmd+Left navigates to the next section (forward in RTL)
- Tab buttons show text only, no icons
</verification>

<success_criteria>
UAT tests 6, 7, 8, and 11 would pass on re-test. Operator panel scores are editable, all shortcut bindings work correctly, and RTL navigation direction matches user expectation.
</success_criteria>

<output>
After completion, create `.planning/phases/05-visual-system/05-06-SUMMARY.md`
</output>
